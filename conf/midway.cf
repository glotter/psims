use.wrapper.staging=false
use.provider.staging=false
provider.staging.pin.swiftfiles=false
wrapperlog.always.transfer=false
sitedir.keep=false
execution.retries=5
lazy.errors=true
status.mode=provider
foreach.max.threads=3000

#app bash=/bin/bash
