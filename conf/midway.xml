<config>
  <pool handle="midway">
    <execution provider="coaster" jobmanager="local:slurm"/>
    <profile namespace="globus" key="jobsPerNode">12</profile>        <!-- Maximum number of RunpSIMS.sh tasks per node -->
    <profile namespace="globus" key="maxTime">86400</profile>         <!-- Maximum time, in seconds of a coaster block -->
    <profile namespace="globus" key="maxwalltime">2:00:00</profile>   <!-- Maximum time, in hh:mm:ss of any one RunpSIMS.sh app call -->
    <profile namespace="globus" key="lowOverallocation">100</profile> <!-- Don't worry too much about these, they should both always be set to 100 -->
    <profile namespace="globus" key="highOverallocation">100</profile>
    <profile namespace="globus" key="queue">westmere</profile>        <!-- Slurm partition to use -->
    <profile namespace="globus" key="slots">20</profile>              <!-- Maximum number of Slurm jobs -->
    <profile namespace="globus" key="nodeGranularity">1</profile>     <!-- Request nodes in blocks of nodeGranularity -->
    <profile namespace="globus" key="maxNodes">1</profile>            <!-- Maximum number of nodes per Slurm job -->
    <profile namespace="karajan" key="jobThrottle">4</profile>        <!-- Multiply this number by 100 to get the total number of active jobs before Swift starts throttling -->
    <profile namespace="globus" key="slurm.exclusive">false</profile> <!-- When set to false, it's easier to get nodes, but things may run slower -->
    <profile namespace="karajan" key="initialScore">10000</profile>
    <filesystem provider="local"/>
    <workdirectory>_WORK_</workdirectory>
  </pool>
</config>
